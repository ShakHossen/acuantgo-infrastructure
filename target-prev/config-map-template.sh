DESIGN_VERSION="${DESIGN_VERSION}"
INSTANCE_VERSION="${INSTANCE_VERSION}"
PROVISIONING_VERSION="${PROVISIONING_VERSION}"
WEBSTORE_VERSION="${WEBSTORE_VERSION}"
K8S_NAMESPACE="${K8S_NAMESPACE}"
SHORT_BRANCH_NAME="${SHORT_BRANCH_NAME}"
DEPLOY_COLOR="${DEPLOY_COLOR}"
K8S_LABEL_DEPLOYMENT="${K8S_LABEL_DEPLOYMENT}"
CONFIG_MAP_NAME="${CONFIG_MAP_NAME}"
SECRET_NAME="${SECRET_NAME}"
DESIGN_SERVICE="${DESIGN_SERVICE}"
INSTANCE_SERVICE="${INSTANCE_SERVICE}"
INSTANCE_PORT="${INSTANCE_PORT}"
DESIGN_PORT="${DESIGN_PORT}"
DESIGN_INTERNAL_PORT="${DESIGN_INTERNAL_PORT}"
INSTANCE_CONFIG_SUB_PATH="${INSTANCE_CONFIG_SUB_PATH}"
INSTANCE_CONFIG_MNT_PATH="${INSTANCE_CONFIG_MNT_PATH}"
INSTANCE_SERVICE_URL="${INSTANCE_SERVICE_URL}"
AWS_REGION="${AWS_REGION}"
AWS_ACCOUNT="${AWS_ACCOUNT}"
AWS_ECR_REGISTRY="${AWS_ECR_REGISTRY}"
DESIGN_IMAGE_REPO="${DESIGN_IMAGE_REPO}"
INSTANCE_IMAGE_REPO="${INSTANCE_IMAGE_REPO}"
PROVISIONING_IMAGE_REPO="${PROVISIONING_IMAGE_REPO}"
WEBSTORE_IMAGE_REPO="${WEBSTORE_IMAGE_REPO}"
DESIGN_REGISTRY_SECRET_NAME="${DESIGN_REGISTRY_SECRET_NAME}"
